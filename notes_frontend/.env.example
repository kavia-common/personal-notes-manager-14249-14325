# Public env variables for the frontend (prefixed per Astro convention)
# The base URL for the backend REST API used by the frontend.
# If not provided, the frontend will default to the same-origin '/api' base.
PUBLIC_API_BASE=https://your-backend.example.com/api
